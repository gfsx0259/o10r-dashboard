# Базовый образ с Node.js и последним npm
FROM node:24.3.0-slim

# Рабочая директория внутри контейнера
WORKDIR /app

# Устанавливаем зависимости (если package.json будет позже, это всё равно нужно)
COPY package*.json ./
RUN npm install

# Копируем остальной проект (необязательно, если монтируем volume)
COPY . .

# Открываем порт, на котором работает Vite (если используется create-vue)
EXPOSE 5173

# Команда запуска в dev-режиме
CMD ["npm", "run", "dev", "--", "--host"]